# No environment variables are required.
#
# These tools work entirely offline on CSV/XLSX files exported from the
# Shopify admin. They never call the Shopify API and read no credentials.
# The Shopify admin login used to export/import the files stays with the
# store owner and is never stored in this repo.
